# Testing Pattern

```
composer update

# to run unit tests
./vendor/bin/phpunit

# to run profiled unit tests
./vendor/bin/phpunit -c phpunit-xhprof.xml

# to run timing tests
./vendor/bin/athletic -p tests
```
